// import styles from "./section.module.css"

const Section = ({title}) => {
    return (
        <>
        <section>
            <h2>{title}</h2>
        </section>
        </>
    )
}

export default Section;